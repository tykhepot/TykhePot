# TykhePot 主网直接上线方案

**决策**: 跳过付费审计，直接主网上线  
**风险等级**: 🔴 高风险  
**执行时间**: 准备完成后 2-4 小时

---

## ⚠️ 风险警告

**您正在做出高风险决策**:

1. **智能合约未经专业审计**，可能存在未发现的漏洞
2. **历史上多个项目因此损失数百万美元**:
   - Wormhole: $320M (合约漏洞)
   - Ronin: $625M (私钥泄露)
   - Cream Finance: $130M (重入攻击)

3. **建议的最低防护措施**:
   - ✅ 已完成免费审计（发现2中等问题，已修复）
   - ✅ 已添加紧急暂停功能
   - ⚠️ 建议设置较小的初始流动性进行测试
   - ⚠️ 建议分阶段开放功能

---

## 上线前检查清单

### 1. 资金准备

| 项目 | 金额 | 用途 |
|------|------|------|
| 部署 Gas | 0.5-1 SOL | 合约部署 |
| 代币创建 | 0.1 SOL | 创建 TPOT 代币 |
| 初始流动性 | 您决定 | SOL + TPOT 配对 |
| 运营储备 | 建议 10+ SOL | 日常操作 |

**总预算建议**: 至少 20 SOL + 流动性资金

### 2. 主网钱包准备

**重要**: 使用新的专用钱包，不要与 Devnet 钱包混用

```bash
# 创建主网钱包
solana config set --url mainnet-beta
solana-keygen new --outfile ~/.config/solana/mainnet-id.json

# 保存助记词（离线保存）
```

### 3. 上线步骤

#### 步骤 1: 配置主网环境
```bash
export PATH="$HOME/.local/bin:$PATH"
solana config set --url mainnet-beta
solana config set --keypair ~/.config/solana/mainnet-id.json
```

#### 步骤 2: 准备资金
- 从交易所提取 SOL 到主网钱包
- 建议至少 20 SOL

#### 步骤 3: 创建代币
```bash
cd smart-contract

# 创建 TPOT 代币
spl-token create-token --decimals 9
# 记录代币地址: TOKEN_MINT

# 创建代币账户
spl-token create-account TOKEN_MINT

# 铸造代币（分批）
# 建议先铸造少量测试，确认无误后再铸造全部
spl-token mint TOKEN_MINT 1000000000  # 10亿 TPOT
```

#### 步骤 4: 部署合约
```bash
# 修改 Anchor.toml 指向主网
# [programs.mainnet]
# tykhepot = "..."

# 构建
anchor build

# 部署（这将消耗约 0.5-1 SOL）
anchor deploy --provider.cluster mainnet

# 记录程序 ID: PROGRAM_ID
```

#### 步骤 5: 初始化合约
```bash
# 修改 scripts/initialize.ts 使用主网参数
npm run initialize:mainnet
```

#### 步骤 6: 配置前端
```bash
cd frontend

# 创建 .env.production
cat > .env.production << EOF
REACT_APP_SOLANA_NETWORK=mainnet
REACT_APP_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
REACT_APP_PROGRAM_ID=YOUR_PROGRAM_ID
REACT_APP_TOKEN_MINT=YOUR_TOKEN_MINT
REACT_APP_STATE_PDA=YOUR_STATE_PDA
EOF

# 构建
npm run build
```

#### 步骤 7: 部署前端
```bash
# 使用 Vercel
vercel --prod

# 或使用 Cloudflare Pages
# 或使用 GitHub Pages
```

#### 步骤 8: 配置域名
```
在 Cloudflare 配置 tykhepot.com 指向 Vercel/部署地址
```

#### 步骤 9: 建立流动性（可选，建议先观察）
```bash
# 在 Raydium/Orca 创建流动性池
# 建议先小金额测试
```

---

## 分阶段上线策略（降低风险）

### 阶段 1: 软启动（第1天）
- 仅开放小时池
- 限制最大投入 1000 TPOT
- 团队内部测试
- 监控异常

### 阶段 2: 小范围测试（第2-3天）
- 邀请信任的用户参与
- 限制参与人数 100人
- 监控合约状态

### 阶段 3: 逐步开放（第4-7天）
- 开放天池
- 开放质押
- 移除投入限制

### 阶段 4: 全面运营（第7天后）
- 开放空投
- 启动推广
- 市场宣传

---

## 紧急预案

### 发现漏洞时
```rust
// 立即执行紧急暂停
await program.methods
  .emergencyPause()
  .accounts({ authority: wallet.publicKey, state: statePDA })
  .rpc();
```

### 资金转移
- 多签钱包预先设置
- 紧急提取脚本准备

### 社区沟通
- 准备公告模板
- 建立 Telegram 公告频道

---

## 最低可行性上线（MVP）

如果资金有限，最小配置：

```
必需:
- 2 SOL (部署 Gas)
- 1,000,000 TPOT (空投池)
- 100,000 TPOT (初始流动性)

可选（逐步添加）:
- 质押池资金
- 推广池资金
- 更多流动性
```

---

## 您的决策确认

**请确认您理解并接受以下风险**:

- [ ] 合约未经专业第三方审计
- [ ] 可能存在未发现的安全漏洞
- [ ] 可能面临资金损失风险
- [ ] 愿意承担由此产生的一切后果
- [ ] 已阅读并理解风险披露文档

**如果以上都确认，我们可以立即开始主网部署**。

---

## 立即执行需要您提供

1. **主网钱包地址**（已准备好 SOL）
2. **准备投入的资金数量**
3. **是否采用分阶段策略**

请告诉我：
- 您准备了多少 SOL？
- 是否采用分阶段上线？
- 是否准备好立即执行？

**一旦确认，我立即开始主网部署流程！** 🚀
